-- ============================
-- CATEGORY_MASTER（区分マスタ）
-- ============================
CREATE TABLE category_master (
    category_id SMALLINT PRIMARY KEY,
    category_name VARCHAR(50) NOT NULL
);
