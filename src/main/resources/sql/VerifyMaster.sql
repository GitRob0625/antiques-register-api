-- ============================
-- VERIFY_MASTER（確認方法マスタ）
-- ============================
CREATE TABLE verify_master (
    verify_id SMALLINT PRIMARY KEY,
    verify_identity VARCHAR(100) NOT NULL
);
